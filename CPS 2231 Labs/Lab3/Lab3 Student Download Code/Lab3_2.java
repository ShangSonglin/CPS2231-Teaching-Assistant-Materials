import java.util.Scanner;
	// TODO: You may add data cleaning module in the main method to only read alpha-numeric character and be case-insensitive
	// You may copy and paste the output from below directly and change "Li Hua" to your name:
    // System.out.println("After Li Hua's testing, " + str1 + " and " + str2 + " are anagrams, which is " + areAnagrams(str1, str2));
	// No ArrayList (stick to arrays)
	// No replace(), replaceAll() methods
public class Lab3_2{
	public static void main(String[] args){
		Scanner sc = new Scanner(System.in);
		sc.close();
	}
	
	// Don't change the method name, add signature only
	// To handle duplicate characters, you can create a boolean array to track which characters in the second string have already been matched or stick to use counters
	areAnagrams(){
		
	}
	//You can create other methods if you want
}
